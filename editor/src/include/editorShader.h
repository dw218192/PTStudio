#pragma once

namespace PTS {
    namespace EditorShader {
        enum VertexAttribBinding {
            Position = 0,
            Normal = 1,
            TexCoords = 2,
        };
        enum UBOBinding {
            LightBlock = 0,
        };
    } // namespace EditorShader
} // namespace PTS