---
alwaysApply: true
---
## Test System Overview
This project uses doctest as the unit test framework. The tests are located in the `tests` folder of a component and are built by default.

## Common Commands
To run the unit tests, run:
```powershell
.\pts.cmd test
```